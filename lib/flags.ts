// Feature flags.
//
// SHOW_VIDEO_SECTIONS: hides every product/automation "video" section site-wide
// while the real videos are not ready yet. Flip to `true` (or gate per page)
// once the videos are uploaded to reveal all the video sections at once.
export const SHOW_VIDEO_SECTIONS = false;
